Yes — same treatment.

And this is the right call. The current pages are still using the older pattern:

* big image
* glass panel
* stacked bullets

That works, but it is not as strong as the **dossier-detail** format for 2026 portfolios, where the best case-study pages lean on **strong hierarchy, data-forward storytelling, minimal presentation, and subtle microinteractions instead of flashy effects**. ([IxDF - Interaction Design Foundation][1])

It also matches your site constraints exactly:

* white/off-white public backgrounds
* Space Grotesk only
* square corners only
* violet used as signal, not fill
* design changes should stay at the CSS/page level, without touching routes/admin/backend. 

So below is the **Replit-friendly implementation brief** for applying the **same Concept C dossier treatment** to:

* `CustomerEngagement`
* `BrandStorytelling`
* `AwardsRecognition`
* `ContentWritingSamples`

---

# Portfolio Detail Pages — Concept C Dossier Treatment — Replit Implementation Guide

### airinjohn.com · React + TypeScript + Tailwind + Framer Motion

---

## BEFORE YOU START — READ THIS

This guide applies the same **dossier-detail system** used for the new Strategic Marketing page to the remaining portfolio detail pages.

### Target pages

* `/portfolio/customer-engagement`
* `/portfolio/brand-storytelling`
* `/portfolio/awards-recognition`
* `/portfolio/content-writing`

### What changes

* page layout only
* visual treatment only
* information architecture only
* card/case-file styling only

### What does not change

* routing
* backend
* database
* admin system
* navbar/footer/HUD overlay usage
* existing public URLs

This follows the project rules already defined for the site. 

---

## WHY THIS IS THE RIGHT 2026 DIRECTION

Current portfolio guidance is pretty consistent:

* case studies should be **easy for hiring managers to scan**
* strong portfolios use **data-driven case studies**
* visual hierarchy should guide the reader through the story
* interactive behaviour should stay **subtle and purposeful**, not distracting. ([IxDF - Interaction Design Foundation][1])

So instead of repeating the old “image + glass panel” layout across every page, each page should become a **structured archive of case files**, with:

* a stronger archive header
* cleaner metadata
* more obvious outcomes
* sharper section labels
* evidence-panel imagery
* KPI strips where applicable
* no glassy softness

---

## GLOBAL RULES FOR ALL FOUR PAGES

Apply the same design system across all pages:

### Keep

* `Navbar`
* `Footer`
* `HudOverlay`
* Framer Motion fade-up entrances
* Wouter `Link`
* existing route/component filenames

### Remove / replace

* `glass-panel` as the main content shell
* generic two-column campaign blocks with loose bullets
* decorative softness

### Use instead

* dossier-style page shell
* archive header
* case-file blocks
* evidence image panel
* segmented content blocks
* data/KPI strip where relevant
* square borders only

---

## USE THE SAME DOSSIER CSS FAMILY

Use the same styling logic introduced for Strategic Marketing.

### Reuse as much as possible

Keep a shared family of classes like:

* `.case-file`
* `.case-top`
* `.case-grid`
* `.case-media`
* `.case-body`
* `.case-block`
* `.case-kpis`
* `.case-pill`
* `.case-reveal`

### Add only page-specific wrappers

Use page wrappers like:

* `.engage-page`
* `.story-page`
* `.awards-page`
* `.writing-page`

But keep the internal dossier module language shared. That will make the portfolio feel like one coherent system instead of four different mini-sites.

---

# 01 — CUSTOMER ENGAGEMENT

## New page direction

This page should feel like:
**community and experience case files**

Not just event summaries.

### The strongest 2026 framing

This page should communicate:

* community building
* activation strategy
* experience-led brand growth
* PR and social amplification
* proof of local traction

### Replace the current structure with:

* archive header
* 2 dossier case files
* each file with:

  * file ID
  * status
  * category/type
  * title
  * brand
  * context
  * objectives
  * execution/activation approach
  * results
  * KPI strip

### Re-map the existing two entries like this

#### Case file 1

**Community Workshops & Events — Wild & The Moon**

* `type`: Community Activation
* `brand`: Wild & The Moon
* `market`: UAE
* `kpis`:

  * `+45%` / `Event Attendance`
  * `+13%` / `Social Engagement`
  * `IRL` / `Activation Format`

#### Case file 2

**In-store Experience — Kava & Chai**

* `type`: Experience Marketing
* `brand`: Kava & Chai
* `market`: UAE
* `kpis`:

  * `+45%` / `Social Engagement`
  * `+65%` / `Foot Traffic`
  * `50+` / `Influencers`

### Header recommendation

* label: `// Case Files · Customer Engagement`
* title: `Customer Experience & Community Engagement`
* subtext:
  “Selected work across community activations, in-store experiences, PR-led buzz, and audience engagement designed to deepen loyalty and drive physical and digital interaction.”

### Metadata strip recommendation

* `02 FILES`
* `Community`
* `UAE`
* `Experience-Led`

---

# 02 — BRAND STORYTELLING

## New page direction

This page should feel like:
**narrative systems and multi-channel campaign files**

This page is especially important because current portfolio guidance puts a lot of weight on **storytelling + outcomes + structure** in case studies. ([IxDF - Interaction Design Foundation][1])

### Replace the current structure with:

* archive header
* mapped dossier case files
* each file should show:

  * campaign type
  * channels used
  * narrative concept
  * objective
  * execution stack
  * results if available
  * KPI strip where possible

### Re-map the existing two entries like this

#### Case file 1

**Brand Campaign — Chin Chin**

* `type`: Multi-Channel Brand Campaign
* `status`: `PLANNED` or `IN DEVELOPMENT` instead of complete
* `market`: UAE
* `channels`: keep as pill row, not just bullets
* `kpis`:

  * `2025` / `Launch`
  * `07` / `Channels`
  * `Brand` / `Campaign`

This file has no final result data yet, so do **not** fake outcomes.
Instead, structure the final block as:

* `Planned Channel System`
* `Campaign Objective`
* `Launch Scope`

That is cleaner and more honest.

#### Case file 2

**CSR: Ramadan Campaign — Chin Chin**

* `type`: CSR Storytelling Campaign
* `market`: UAE
* `kpis`:

  * `23k+` / `Vouchers`
  * `9M+` / `Impressions`
  * `11` / `PR Hits`

Optional fourth signal can be folded into the result list:

* `600+` social stories

### Header recommendation

* label: `// Case Files · Brand Storytelling`
* title: `Brand Storytelling & Multi-Channel Marketing`
* subtext:
  “Selected campaign work spanning brand narratives, seasonal storytelling, multi-channel distribution, and culturally relevant messaging designed to build visibility, affinity, and campaign momentum.”

### Important content rule

For this page:

* **channels should become pills or a structured grid**
* not long list-only formatting

Because this page is about orchestration and narrative architecture, not just campaign summary.

---

# 03 — AWARDS & RECOGNITION

## New page direction

This page should **not** stay as one big glass card.

It should become:
**credential archive / recognition dossier**

This page is less about storytelling depth and more about **credibility signals**, which is still valuable in recruiter-facing portfolios. Strategic curation is one of the strongest traits noted in current portfolio guidance. ([IxDF - Interaction Design Foundation][1])

### Replace current structure with:

* archive header
* one main dossier block for `Wild & The Moon`
* inside the block:

  * file ID
  * status
  * recognition category
  * context summary
  * awards grouped by year
  * impact summary
  * KPI strip

### Recommended KPI strip

* `07+` / `Awards`
* `2020–2022` / `Timeline`
* `GCC` / `Recognition`

### Header recommendation

* label: `// Case Files · Recognition Archive`
* title: `Awards & Recognition`
* subtext:
  “Selected awards, nominations, and recognition signals that strengthened brand credibility, regional visibility, and category leadership.”

### Content treatment change

Instead of:

* awards inside nested bullet blocks
* extra glass panel inside another glass panel

Use:

* a main dossier shell
* year dividers as clean archive rows
* one final impact block
* no inner glass box

### Better internal structure

* `Context`
* `Awards by Year`
* `Impact on Brand Positioning`
* `Recognition Signals`

This page should feel like a **clean record**, not a campaign case study.

---

# 04 — CONTENT WRITING SAMPLES

## New page direction

This page should become:
**document archive / writing library**

This one is different from the others.

It should still use the dossier system, but instead of campaign case files, it should present:

* category dossiers
* document tiles
* cleaner archive logic

### Why this matters

Current portfolio best practice keeps stressing:

* organized structure
* minimal presentation
* high-quality examples
* strong navigation/readability. ([IxDF - Interaction Design Foundation][2])

Your current page is already closer to a library, but the visual language is still too “component-y” and too dependent on glass panels and generic cards.

### Replace current structure with:

* archive header
* category summary strip
* category dossier sections
* document tiles styled as file entries

### Recommended page structure

1. header
2. top metadata strip:

   * total sample count
   * 3 categories
   * writing archive
   * public documents
3. category sections:

   * `Press Releases`
   * `Award Submissions`
   * `Email Marketing`
4. each sample should render as a **document file tile**

### Each sample tile should show:

* file ID
* document title
* file type
* category tag
* `Read File →`

### Suggested tile treatment

Instead of the current generic card:

* make each tile look like a mini dossier/file sheet
* flat bordered panel
* top file ID line
* small category label
* title
* CTA row
* subtle hover border + iris signal
* no icon overload

### Keep icons?

You can keep the category-level icons if you want, but:

* reduce their visual dominance
* they should support the category heading, not compete with the content

### Header recommendation

* label: `// Document Archive · Writing Samples`
* title: `Content Writing Portfolio`
* subtext:
  “A structured archive of press releases, award submissions, and email marketing work demonstrating strategic messaging, editorial precision, and brand voice consistency across formats.”

### Metadata strip recommendation

* `15 FILES`
* `03 CATEGORIES`
* `Docs`
* `Writing Archive`

---

# CONTENT MODEL CHANGES RECOMMENDED

For Replit, ask it to normalize the data across pages.

## Customer Engagement

Turn entries into:

```ts
{
  fileId,
  status,
  type,
  brand,
  market,
  title,
  subtitle,
  description,
  objectives,
  execution,
  results,
  kpis,
  image
}
```

## Brand Storytelling

Turn entries into:

```ts
{
  fileId,
  status,
  type,
  market,
  title,
  subtitle,
  description,
  objectives,
  channels,
  results,
  kpis,
  image
}
```

## Awards

Turn into:

```ts
{
  fileId,
  status,
  title,
  organisation,
  summary,
  years: [
    { year: "2020", items: [...] },
    { year: "2021", items: [...] }
  ],
  impact,
  kpis,
  image
}
```

## Content Writing

Turn into:

```ts
{
  id,
  fileId,
  title,
  category,
  pdfFile,
  fileType
}
```

---

# INTERACTION RULES FOR ALL FOUR PAGES

Use the same interaction logic as Strategic Marketing:

* Framer Motion fade-up on entry
* case/dossier blocks reveal on scroll
* image panels have subtle scan-line on hover
* title spacing shifts slightly on hover
* KPI/data blocks remain calm and readable
* mobile should not depend on hover

### Do not add

* dramatic parallax
* large clip-path theatrics
* rotating HUD elements
* oversaturated violet
* nested glass panels

That lines up with the broader 2025–2026 direction toward clean, structured, purpose-driven interactions. ([Webflow][3])

---

# VISUAL SYSTEM REMINDER FOR REPLIT

Apply all of this within the existing site rules:

* **Space Grotesk only**
* **No rounded corners**
* **White/off-white backgrounds only**
* **Violet only for signal moments**
* **Green only for the status indicator chips / system signal use**
* **No routing changes**
* **No admin/backend changes** 

---

# REPLIT EXECUTION SUMMARY

Send this to Replit as the condensed implementation brief:

### Implement exactly this:

* apply the same **Concept C dossier-detail treatment** used for Strategic Marketing to:

  * `CustomerEngagement`
  * `BrandStorytelling`
  * `AwardsRecognition`
  * `ContentWritingSamples`
* do not change routing, admin, backend, or DB
* keep `Navbar`, `Footer`, and `HudOverlay`
* replace the current `glass-panel`-driven layouts with dossier-style archive layouts
* use a shared dossier component language across all pages
* surface results and proof more clearly
* convert writing samples into file-style document tiles
* keep all public pages white/off-white, square-edged, minimal, and recruiter-readable
* keep motion subtle and purposeful
* keep violet restricted to signal/accent states only

---

[1]: https://www.interaction-design.org/literature/article/the-10-most-inspirational-ux-design-portfolio-examples?srsltid=AfmBOora5w8v2XsPUpXSS1in_1CfwXQMbsvaPFXhA9bA3o9TovujjnI5&utm_source=chatgpt.com "The 10 Most Inspirational UX Design Portfolio Examples in ..."
[2]: https://www.interaction-design.org/literature/topics/interactive-design-portfolios?srsltid=AfmBOoqB3kqAyMNt75ad1NmMzSW613I-JBYei7aREq8C9Xsf80gwIZmB&utm_source=chatgpt.com "Interactive Design Portfolios"
[3]: https://webflow.com/blog/microinteractions?utm_source=chatgpt.com "15 best microinteraction examples for web design inspiration"
